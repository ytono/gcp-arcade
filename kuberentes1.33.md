GKE 1.32.4 から 1.33 へのバージョンアップに伴うノードの Containerd 2 への移行に関して、お客様の環境への影響についてご説明いたします。

GKE 1.33 では、基盤となるコンテナランタイムが Containerd 1.7 から Containerd 2.0 へと移行されます。この移行は GKE のバージョンアップの一部として自動的に行われるため、お客様側で Containerd 自体を個別に移行する作業は不要です。GKE のバージョンアップによってノード上の Containerd のバージョンも自動的に更新されます。

ただし、この移行には以下の潜在的な影響があります。

1.  **非推奨機能の互換性問題**:
    GKE 1.32 が使用する Containerd 1.7 では、Docker Schema 1 イメージおよび Container Runtime Interface (CRI) v1alpha2 API が非推奨となっていました。 GKE 1.33 が使用する Containerd 2.0 では、これらの非推奨機能のサポートが完全に削除されます。
    現在お客様のワークロードでこれらの古い機能が利用されている場合、GKE は自動アップグレードを一時停止する可能性があります。 もし非推奨機能の修正を行わずに 1.33 へとアップグレードされた場合、Docker Schema 1 イメージを使用しているワークロードが動作しなくなる可能性があります。 アップグレード前に、GKE の「Cloud Recommendations」で非推奨機能の使用状況を確認し、必要に応じてワークロードを修正することが強く推奨されます。

2.  **ファイルオープン数のデフォルト制限の引き下げ**:
    GKE 1.33 で導入される Containerd 2.0 では、コンテナのファイルオープン数のデフォルトのソフトリミット (`ulimit -n`) が 1024 に引き下げられます。 以前のバージョンではこれより高い値が設定されていたため、より多くのファイルオープン数を必要とするワークロードは「Too many open files」エラーに遭遇する可能性があります。

    **対策**:
    *   アプリケーションレベルで `ulimit -n` または `prlimit --nofile` を明示的に設定して、ファイルオープン数の制限を増やす。
    *   `containerd/nri ulimit-adjuster plugin` を使用して `ulimit` を調整する。

**まとめ**:
お客様のGKE環境が1.32.4から1.33へバージョンアップする際には、ノードのContainerdが1.7から2.0へ自動的に移行します。この移行自体にお客様による特別な操作は不要ですが、以下の点にご注意ください。

*   **現在、Docker Schema 1 イメージまたは CRI v1alpha2 API を使用しているワークロードがないか確認し、使用している場合は 1.33 へのアップグレード前に修正してください。** GKE の Cloud Recommendations をご確認ください。
*   **ワークロードがデフォルトのファイルオープン数の制限 (1024) で問題なく動作するか確認してください。** 必要に応じてアプリケーションまたはコンテナの設定で制限を上げることをご検討ください。

これらの点をご確認いただくことで、1.33へのスムーズなバージョンアップとワークロードの安定稼働が期待できます。

＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝＝
GKE 1.32.4 から 1.33 へのバージョンアップに伴う Linux cgroupv2 への移行に関して、お客様の環境への影響についてご説明いたします。

**cgroupv2 へのデフォルト移行とお客様環境への影響**

GKE 1.33 以降では、新しく作成されるノードプールおよび既存のノードプールにおいて、cgroupv1 から cgroupv2 への移行がデフォルトで自動的に行われます。お客様の環境が現在 1.32.4 を利用しており、1.33 へのバージョンアップを計画されている場合、この自動移行の対象となります。現在 cgroupv1 を使用しているクラスターは、1.33 へのアップグレード時に cgroupv2 に移行されます。

cgroupv2 は、Linux カーネルのコンテナリソース管理機能の新しいバージョンであり、リソース分離の改善や新しい機能を提供します。Kubernetes はバージョン 1.25 で cgroupv2 を GA (General Availability) にしており、GKE もバージョン 1.26 以降で新しいノードプールでは cgroupv2 がデフォルトになっています。

ほとんどの Linux アプリケーションやワークロードは cgroupv2 への移行によって影響を受けないとされていますが、以下の点に注意が必要です。

**潜在的な影響と対策:**

1.  **モニタリングツールおよびエージェント:**
    *   `/sys/fs/cgroup` パスを直接パースする、または cgroupv1 特有のパスに依存するサードパーティ製のモニタリングツールやエージェントは、cgroupv2 に対応するためにアップデートが必要になる場合があります。
    *   例として、cAdvisor をスタンドアロンの DaemonSet として実行している場合は、v0.43.0 以降に更新する必要があります。

2.  **cgroup パスに直接アクセスするアプリケーション:**
    *   アプリケーションが `/sys/fs/cgroup` を直接読み込む、または cgroupv1 の構造を期待している場合、予期せぬ動作をしたり、失敗する可能性があります。cgroupv2 は cgroupv1 とは異なる API を使用しています。
    *   Node-level のメモリ使用量が cgroupv1 と比較して cgroupv2 で高く報告されるという既知の動作変更があります (250-750 Mi 程度)。これはノードレベルの統計のみに影響し、Podレベルの統計には影響しません。

3.  **古い言語ランタイムやライブラリ:**
    *   古いバージョンの Java, Python, Node.js, glibc などの一部の言語ランタイムやライブラリでは、cgroupv2 環境下でリソース使用量を正確に報告できなかったり、cgroup ファイルシステムとの連携に問題が生じる可能性があります。
    *   これらのランタイムやライブラリを新しいバージョンにアップデートすることが推奨されます。例えば、Java の場合、OpenJDK / HotSpot の jdk8u372, 11.0.16, 15 以降のバージョンでは cgroupv2 が完全にサポートされています。

**推奨される対策:**

*   **事前テスト:**
    *   本番環境へのアップグレードを行う前に、GKE 1.33 (またはそれ以降) を使用するステージング/開発クラスターでワークロードが期待通りに動作するかを十分にテストしてください。
    *   現在のクラスターの cgroup モードは、`gcloud container clusters describe CLUSTER_NAME --format='value(nodePools[0].config.effectiveCgroupMode)'` コマンドで確認できます。

*   **モニタリングツールの確認と更新:**
    *   使用しているすべてのサードパーティ製モニタリングツールやセキュリティエージェントが cgroupv2 と互換性があることを確認し、必要に応じて最新バージョンに更新してください。

*   **アプリケーションの見直し:**
    *   アプリケーション内部で cgroup ファイルシステムに直接アクセスしている箇所がないかを確認し、もしある場合は cgroupv2 に対応したバージョンに修正してください。

*   **言語ランタイムの更新:**
    *   利用している言語ランタイムが cgroupv2 に対応しているかを確認し、必要に応じて最新の安定版に更新してください。

**GKE の自動移行と一時的なオプトアウト:**

GKE 1.33 では cgroupv1 から cgroupv2 への自動移行が行われますが、一時的にこの自動移行を回避することも可能です。これは、cgroupv1 を明示的に指定することで実現できます。ただし、これは一時的な措置であり、長期的な解決策ではありません。

**まとめ:**

お客様の GKE 環境が 1.32.4 から 1.33 へバージョンアップする際には、ノードの cgroup モードが cgroupv1 から cgroupv2 へ自動的に移行します。この移行自体は GKE によって管理されますが、お客様のワークロードや使用しているツールによっては影響を受ける可能性があります。特に、監視ツール、cgroup ファイルシステムに直接アクセスするアプリケーション、および古い言語ランタイムについては互換性の確認と必要に応じた更新を強く推奨します。事前にテスト環境で十分な検証を行うことで、スムーズなバージョンアップとワークロードの安定稼働が期待できます。
